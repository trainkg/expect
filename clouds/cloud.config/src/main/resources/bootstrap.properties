spring.application.name=configserver
encrypt.fail-on-error=false
encrypt.key-store.location=classpath:keystore.jks
encrypt.key-store.password=${KEYSTORE_PASSWORD:foobar} # don't use a default in production
encrypt.key-store.alias=test